{
  "name": "boop",
  "version": "1.0.0",
  "scripts": {
    "build": "cd Frontend && npm install && npm run build",
    "start": "cd Backend && npm install && node src/index.js"
  },
  "engines": {
    "node": "20.x"
  }
}